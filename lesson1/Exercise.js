/*
1.  Create a function called fn1 as shown (Tip : function fn1(){} ).

2.   Log the string as scope 1 . (Tip: console.log("scope _") )

3.  Create a variable called example with the value of 5. (Tip: var example = _ )

4.  Log the value of the variable called example. ( Tip: console.log( examp... ))

5.  Create a new block scope inside of the function with curly braces (Tip: within the function just type this {} ).

6.  Inside the new scope block, log the string called scope 2. (Tip: { console.log("scope _ ") } )
*/
